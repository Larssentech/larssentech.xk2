package org.larssentech.xkomm2.ui.shared.chat;

import javax.swing.JLabel;

public class StreamingZtatuzBar extends JLabel {

	public StreamingZtatuzBar(String initialText, int alignment) {

		super(initialText, alignment);

		// this.setText(initialText);

		// this.setBackground(Color.blue); // Xkomm2Theme.getBackground());
		// this.setOpaque(true);
	}

}
